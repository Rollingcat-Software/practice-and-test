# Ayşe Gülsüm EREN

Ayşe Gülsüm EREN's contributions are not isolated to a separate branch
— they are interleaved into Aysenur15's branches:

- `working_spoof_detection` — 1 commit (`13243a1` P0 FRR reduction)
- `fix/liveness-cascade-frr-reduction` — lead author (head-turn / nose-occlusion fix)
- `fix/liveness-p0-frr-reduction` — lead author (P0/P1/P2/cascade-guard tuning)
- `fix/liveness-p3-frr-reduction` — co-author

See those branch folders under `../aysenur15/` and the master
`../ATTRIBUTION.md` for the full breakdown.

This folder is kept as a placeholder so future contributions that come
in cleanly attributed to her alone (separate branch, distinct subsystem)
can land here.
